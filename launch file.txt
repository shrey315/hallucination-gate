curl -X POST http://127.0.0.1:8000/v1/answer ^
  -H "x-api-key: 6DFMecf2lHZbHqm2Rq38IuHaewNL9OGwLvm8IJOMdhI" ^
  -H "Content-Type: application/json" ^
  -d "{\"query\":\"What is the refund policy?\",\"answer\":\"Refunds are never allowed.\",\"context_chunks\":[\"Customers may request a refund within 30 days.\"]}"


  cd "E:\Baysian Optimization"
uvicorn bayesian_rag_evaluator.api.main:app --reload --host 0.0.0.0 --port 8000

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Those five items are now in the codebase. **29 tests pass.** Here is what you actually have, without overselling it.

**1. Calibration (2,000 labeled cases)**  
Synthetic gold set at `data/gold/claims_2000.jsonl` with `pass` / `rewrite` / `abstain`. CPTs were learned and saved to `config/learned_bn.pkl`.

```bash
rag-eval generate-gold --n 2000
rag-eval calibrate --n 2000 --output-model config/learned_bn.pkl
```

These labels are **template-generated**, not RAGTruth. Swap in real claims when you have them.

**2. Batch + cache**  
Embeddings are computed once, top-8 chunks are selected, NLI runs in a batch, LRU cache hits repeated pairs. That is the p95 path; heuristic CI still does not use MiniLM.

**3. Real multimodal**  
- PDFs: `pypdf` via `pdf_paths`  
- CLIP: `images[].path` → `clip-ViT-B-32` when available  
- OCR: `pip install -e ".[ocr]"` plus a local Tesseract binary  

Without those extras, it still uses caption/OCR text fields.

**4. Gold-set metrics**

```bash
rag-eval eval-gold --n 300
```

Reports precision/recall/F1 of **release**, false-abstain rate, and false-release rate.

**5. Serving**  
Public clients should call **`POST /v1/answer`**. It returns only:

`{request_id, safe_answer, released, latency_ms}`

Never the raw model text.

```bash
set RAG_EVAL_API_KEY=your-secret
set RAG_EVAL_TIMEOUT_SEC=8
uvicorn bayesian_rag_evaluator.api.main:app --host 0.0.0.0 --port 8000
```

`GET /metrics` exposes p50/p95 latency, errors, and gate actions. Missing `x-api-key` returns 401 when the env var is set.

**Still true:** this is production-shaped, not Anthropic-grade. CLIP/OCR need extra installs, the 2k set is synthetic, and neural p95 depends on GPU/CPU and model download. The lock that matters in production is already there: users only ever see `safe_answer`.